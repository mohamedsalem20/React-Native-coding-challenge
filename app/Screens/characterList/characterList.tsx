import React from 'react';
import {StyleSheet, Text} from 'react-native';

const CharacterList = () => {
  return <Text>CharacterList Screen</Text>;
};

const styles = StyleSheet.create({});

export default CharacterList;
